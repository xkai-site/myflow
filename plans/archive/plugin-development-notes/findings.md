# Findings
## Important correction: OpenAI Official Codex route is auth passthrough
- User clarified current CC Switch route: Codex enabled, OpenAI Official OAuth account selected. Parent fetched v3.20.3 src-tauri/src/proxy/providers/codex.rs and forwarder.rs.
- is_codex_official_provider identifies official/managed-account cards; forwarder sets codex_official_auth_passthrough for AppType::Codex, validates caller Authorization (rejects PROXY_MANAGED), and for bound managed accounts validates chatgpt-account-id plus matching bearer session. Header forwarding explicitly preserves inbound Authorization; authentication belongs to the client.
- Thus the prior generic openai-responses + placeholder apiKey suggestion is NOT a valid complete recipe for this concrete route. Router existence and upstream OAuth account selection do not establish gateway-owned auth injection. Earlier delegated report did not inspect this forwarder distinction deeply enough.
- Distinct supported mode: Claude-tab Codex preset uses AuthStrategy::CodexOAuth; forwarder resolves/refreshes token through CodexOAuthManager and injects account/session headers. This is the documented Messages→Codex subscription bridge and the better target for a generic API client (Pi anthropic-messages, server root URL), subject to actual integration verification.
- No secrets read or transferred, no local route calls, no app/config changes. Sources: https://github.com/farion1231/cc-switch/blob/v3.20.3/src-tauri/src/proxy/providers/codex.rs and https://github.com/farion1231/cc-switch/blob/v3.20.3/src-tauri/src/proxy/forwarder.rs .
## Delegated subscription-to-API decision analysis
- User confirms subscription-to-API (not Platform API billing) and requests three evidence-backed answers: CC Switch reuse/multi-client support; avoiding a bloated multi-model manager; impact on pi-image-generation credential reuse.
- User authorizes subagent analysis only. Parent owns planning files; children inspect read-only and return managed reports. No real auth files/databases, model calls, image calls, app takeover, login changes, or implementation authorized.
- Native researcher/reviewer agents launched with fresh contexts and distinct evidence scopes. Existing dirty files are prior planning notes and unrelated deletions.
- Parent inspected running process executable version metadata only: D:/CCSwitch/cc-switch.exe ProductVersion=3.20.3 and FileVersion=3.20.3. This establishes installed version, not feature activation, credential state or client interoperability.
- Workflow receipt confirms all three fresh-context children completed successfully; all reports read. Researcher explicitly incorporated installed 3.20.3 and checked version-tagged sources, so the version update is reflected in final evidence.
- CC Switch report: native Codex OAuth reverse proxy shipped in 3.13.0; 3.20.3 supports documented Claude Messages→Codex Responses flow, independent ~/.cc-switch/codex_oauth_auth.json, and local routing. /responses, /chat/completions, /models and /images routes exist but are not interchangeable proof of subscription compatibility; generic Chat translation and OpenAI models-list shape have limitations. Parent primary-source checks pending.
- Image report: fixed openai-codex auth provider via modelRegistry, not active chat model nor source-package imports; expects official Codex HTTPS base URL and OAuth JWT/account claim. Preserving the old auth provider allows a separately named chat gateway without image source changes. Replacing same-id auth/URL or routing images through gateway would require changes. Uninstall may restore built-in provider, but no guarantee of live Codex-file semantics.
- Architecture report: prioritize mature gateway over self-build; separate account refresh, protocol routing and capability catalogs. Keep chat/image/video/embedding capabilities distinct and avoid all-in-one management UI, automatic account pools or silent fallbacks. Managed reports under session subagent-artifacts/outputs/c9ba5d62-ee2f-45c2-abda-d2483a43db74/analysis/.
- Parent primary checks: fetched v3.20.3 tagged guide/server.rs/handlers.rs/release note. Guide explicitly documents ChatGPT device-code login on Claude tab, independent ~/.cc-switch/codex_oauth_auth.json with automatic refresh, Messages↔Responses bridge and 127.0.0.1:15721; only Claude takeover can be enabled. It does not establish arbitrary client protocol compatibility or true one-copy credential reuse.
- Parent verified current image code: resolveImageAuth gets configured provider by id and validates resolved base URL (credentials.ts:43–74); request builder extracts account from OAuth JWT and posts JSON Images paths (openai-codex-images.ts:20–79); schema locks OpenAI auth to openai-codex (model-config.ts:99–104). New chat provider leaves this path alone; pointing old id at localhost/local key would fail before request.
- Tagged server/handlers confirm Images generation/edit routes already exist in 3.20.3; do not repeat an inaccurate claim they are absent. Their presence does not make current image plugin able to use localhost, nor prove end-to-end subscription/model compatibility.
- Further parent verification: 3.20.3 proxy docs explicitly describe multiple applications, active connections and default loopback binding; router spawns connections and has no global inbound auth middleware in the inspected build_router. Treat this as a local routing service, not independent-key/tenant isolation. Generic Chat handler forwards to /chat/completions; models returns Codex top-level models, not guaranteed OpenAI data shape.
- Current tagged official-auth-preservation guide explicitly says option is off by default and provider switching can overwrite Codex auth unless enabled; only config-only write with preservation is documented for third-party providers. Recommend no Codex takeover during initial evaluation rather than asserting byte-for-byte preservation under every operation.
- Final recommendation adjusts prior self-build-first suggestion: CC Switch already handles subscription routing, so reuse/validate it first. Keep gateway OAuth in its own authorized session and retain old Codex auth for images; CC Switch independent login/refresh does not automatically refresh or switch the original image account. One subscription does not require one shared credential file; avoid copying a refresh token to multiple independent refreshers.
- Important primary URLs: https://github.com/farion1231/cc-switch/blob/v3.20.3/docs/guides/claude-codex-routing-guide-en.md ; https://github.com/farion1231/cc-switch/blob/v3.20.3/docs/user-manual/en/4-proxy/4.1-service.md ; https://github.com/farion1231/cc-switch/blob/v3.20.3/docs/guides/codex-official-auth-preservation-guide-en.md ; tagged src-tauri/src/proxy/server.rs and handlers.rs. No public deployment or account policy permission inferred from project marketing.
## Codex reusable API feasibility
- User asks whether existing work/scripts/pi/pi-codex-official can become an API usable outside Pi; this is exploratory, not authorization to implement or send live requests.
- Target package contains README.md, package.json, config, extensions and test; no applicable AGENTS.md found along the project path or inside the package.
- Existing unrelated deleted interview files and historical planning notes must be preserved.
- Package README explicitly describes a direct Pi openai-codex-responses transport to https://chatgpt.com/backend-api, not Codex CLI forwarding. Plugin only adapts local ChatGPT OAuth and cached models, never refreshes/writes Codex tokens.
- README distinguishes zero-price placeholder metadata from actual subscription quota, and requires external Codex/CC Switch refresh. Need source confirmation; API wrapper would not convert subscription credentials into public API billing.
- Installed Pi README, custom-provider.md and providers.md read completely. Source index.ts confirms openai-codex / openai-codex-responses / https://chatgpt.com/backend-api with no HTTP listener or custom transport; package has no server entry.
- codex-auth.ts has only type-level Pi dependencies and synchronous file reads; validates ChatGPT mode/account consistency/expiry >60s. getApiKey re-reads current auth each request; refreshToken only re-reads, never exchanges refresh tokens.
- codex-models.ts is an extractable pure projection plus disk read (Pi types only); index.ts supplies Pi built-in metadata. Model cache filters visibility=list and supported_in_api=true; this field does not certify public API entitlement.
- Suggested approach: retain existing working plugin, extract shared auth/model core, add independent authenticated loopback HTTP adapter. Reuse pi-ai only as optional headless transport or implement a dedicated Codex transport; do not launch the Pi Agent/TUI or expose local filesystem tools for a model API.
- Official https://developers.openai.com/codex/auth fetched and read: ChatGPT sign-in is subscription access; API-key sign-in is usage-based Platform billing instead of included plan credits. General OpenAI API calls require Platform API keys; docs advise not exposing Codex execution publicly/untrusted. This supports distinguishing a self-hosted adapter from official public API access, not certifying arbitrary proxy use.
- Official Help subscription-transfer article fetch was HTTP 403; did not retry or bypass. Installed Pi models.md read completely; existing provider examples inspected.
- Inspected installed transport request/header/URL slices: exact upstream resolves to https://chatgpt.com/backend-api/codex/responses with store:false, stream:true, normalized instructions/input and Bearer plus chatgpt-account-id. Thus an OpenAI-compatible gateway needs request/event adaptation, not only a URL change. Non-streaming client support could aggregate upstream SSE; official API storage/other endpoints cannot be promised.
- Recommendation: separate codex-api-bridge (working name), preserving the Pi plugin; shared auth/model core and independent transport/server. Begin with loopback plus separate gateway key, cached /v1/models and a documented /v1/responses subset; add /v1/chat/completions for target-client compatibility. Function calls are returned to clients, never executed as local tools by this model gateway.
- Benefits: common Base URL/key across compatible clients, no Pi CLI/TUI process required. Trade-offs: resident service, transport upkeep, token-expiry handling, shared subscription limits; no faster models or official API credits. Initially keep Codex auth read-only/external refresh to avoid multiple refresh-token writers.
- Feasibility only: no implementation, client integration proof, tests, model inference or credential access; public/shared deployment and managed-workspace policies need separate consideration.
## Video output parity
- Video already persists generated-video entries, but renders only filename/model/task metadata; success notification exposes a plain filesystem path.
- Image uses encoded pathToFileURL links, theme underline/mdLink and host OSC 8 capability detection, with one persistent TUI link and plain URLs in non-TUI notifications.
- All video generate/resume success paths share publishOutcome; presentation-only change can cover both without touching generation/download/configuration.
## Direct-image link simplification
- User approved removing HTML output; opening via terminal/OS default application is acceptable, no browser guarantee.
- Current gallery.ts embeds static HTML/CSS; publishOutcome writes a gallery even for one image. Existing preview-link.ts already handles encoded file URLs and OSC 8.
- Replaced gallery links with per-image links derived from persisted path; old galleryPath fields are ignored without migrating history. Preserved TUI image preview, Saved paths and plain RPC URLs.
- Existing Pi entry-renderer pattern and host Text/OSC 8 handling support this without new APIs/dependencies. Read extensions/TUI/session-format/theme docs and renderer examples. Host tests verify publication does not write files even with an unusable cwd and preserves historical HTML/image contents.
## Current c13c87fc read-only diagnosis
- Exact log begins 2026-09-09T18:29:56.737Z (UTC+8 02:29:56.737), fails at approximately 02:30:57.737 after61000ms. Sunburst, auto/auto, zero reference images. TLS socket assigned at2912ms; final bodySent2919ms; peer-close60990ms; no response headers. Sending completed locally, not proof backend accepted/finished generation.
- All14 available logs:11 UND_ERR_SOCKET, spanning Image2/Flare/Sunburst, clustered60143–61405ms;3 HTTP429. No success logs available. Cannot statistically establish prompt-length/size dependence because prompt lengths and successful comparisons are absent.
- Current adapter sends one JSON POST with n:1, no stream, no retry. Prompt max32000 checked before network;48MiB limit applies after response. Neither is the observed no-headers error. Two request-created/bodySent groups are compatible with proxy CONNECT + POST, not proof of duplicate generation.
- Existing host fixture evidence must retain limits: synthetic HTTP direct/tunnel >70s passed; actual Clash HTTPS fixture was REJECTed beforeTLS. No proof actual proxy/backend can wait >60s.
- Fresh proxy correlation: logs/2026-09-10-0209.log:6537 at02:29:57.397+08 routes localhost:8224 -> chatgpt.com:443 through ChatGPT group / 美国LA-优化2-GPT. This is660ms after request start and a strong temporal candidate, not unique attribution (plugin omits local socket port; other ChatGPT connections exist). No matched close/timeout entry near02:30:57; unrelated Microsoft DNS errors do not explain this POST.
- Generated-image directory contains filenames tagged Sunburst at2026-09-09T06-40-52 and Flare at2026-09-09T16-21-22, plus Image2 outputs. These are historical saved-output clues, not wire model attestations or controlled long-vs-short comparisons; avoid declaring new models categorically unsupported. No image/prompt contents read.
- Re-read installed dispatcher: default300000ms for headers/body, global matching Undici fetch unless deliberate override. Current TUI passes loader.signal; non-TUI300000ms. This is source/previous-fixture evidence, not current in-memory dispatcher inspection.
- Prioritized hypothesis: longer generation/nonstreaming waiting exposes an approximately60s cutoff somewhere along real proxy/upstream/backend route. Current log cannot distinguish absolute connection deadline from idle/first-byte deadline: post-body waits vary57.6–59.7s. No proof of prompt-byte overflow, backend completion, exact closer, or streaming support/fix.
## Actual Clash local HTTPS fixture blocked
- User confirms running Pi settings shows5min. Local HTTPS probe used installed host dispatcher via actual127.0.0.1:7897 with ephemeral certificate trusted only through child NODE_EXTRA_CA_CERTS. No TLS verification bypass, certificate-store/settings changes, credentials or external requests.
- Attempt failed36ms ECONNRESET before secure TLS, zero POSTs received by local server, one CONNECT to actual7897. Clash log2026-09-10-0003.log line8294 at01:16:27.4185648+08 explicitly routes loopback test to REJECT via IPCIDR(127.0.0.1/32). Thus this fixture did not exercise70s delay and says nothing about production60s closure or remote nodes. Do not conflate failures.
- Temporary certificate/key deleted; evidence plans/clash-https-probe-result.json, explicit script test/clash-https-probe.mjs. No retry or proxy-rule bypass performed. A narrowly scoped temporary rule would require separate approval; not justified as an image fix.
## Host dispatcher follow-up (current installed runtime)
- Installed host uses npm Undici 8.9.0; configureHttpDispatcher sets EnvHttpProxyAgent, headersTimeout/bodyTimeout 300000 by default, allowH2:false, proxyTunnel:true, installs matching npm fetch unless deliberately overridden. No 60-second deadline found in inspected client/proxy path; Undici Client fallback timeouts are also 300000.
- main.js configures dispatcher initially and after settings; interactive mode can reconfigure on reload/settings change. Disk user httpIdleTimeoutMs unset, project settings absent. Sanitized inherited uppercase/lowercase HTTP(S) proxy variables all point to local HTTP7897; NO_PROXY absent. This is child environment/disk evidence, not a current-Pi heap inspection.
- Previous standalone Node default-fetch loopback test succeeded after70056ms with headers at70035ms, but did not initialize the host dispatcher or proxy/TLS.
- New actual installed-host dispatcher tests passed direct70108ms (headers70098ms) and local CONNECT tunnel70147ms (headers70125ms). Asserted global fetch identity matches host Undici.fetch, default300000ms, one POST each; tunneled case had exactly one additional CONNECT. Request-level timeout overrides were null (inherit client defaults, not disabled). This reproduces two request-created/body-sent groups without duplicate generation.
- Reproducible opt-in script test/host-dispatcher-probe.mjs, results plans/host-dispatcher-direct-result.json and plans/host-dispatcher-tunnel-result.json. Each child changes only its own proxy environment to local fixture; no real proxy or external connection. No TLS bypass or production settings changes.
- This does not establish that current Pi heap/overridden fetch matches new process, nor test TLS/Clash/provider. Interactive settings UI reads settingsManager.getHttpIdleTimeoutMs; changes invoke configureHttpDispatcher. Viewing current HTTP idle timeout is a low-risk remaining in-process configuration check, though not a direct dispatcher heap inspection.
## GPT Image 2.5 test assumption correction
- User explicitly enabled both models and requested removing obsolete rejection assertions. Removed model.enabled===false and enabled Flare-as-default rejection; added positive acceptance of both as defaults.
- Command and host transport tests now use actual config, not a clone forcibly setting every enabled flag. Generic disabled-model tests use a synthetic disabled-test-model fixture and cannot pass vacuously when all real models are enabled.
- Host wizard previously assumed one auto-selected OpenAI model and prohibited Sunburst/Flare labels. It now verifies both can be selected and advance to size selection without any auth/generation call.
- Full verification: 36 unit tests + host suite + 30 baseline request comparisons passed. Production source/config unchanged in this follow-up.
## User-approved live probe
- Actual adapter with current Codex credentials and host dispatcher sent exactly one GPT Image 2 POST. HTTP 429 `The usage limit has been reached` returned in 1766 ms (1792 ms total). No image or retry. This establishes current usage-limit blocking, NOT the earlier UND_ERR_SOCKET root cause.
- Result: plans/openai-image-live-probe-result.json; log error-5db73ba5-7b06-4923-b547-47f19cb39436.json. No credential contents printed/stored.
- Codex CLI 0.153.4 image_generation enabled, generated protocol has no direct image RPC. Official-client comparison not executed; stop further paid probes until usage limits are resolved.
## Current official-interface investigation
- Newly supplied error-4f3a23b9-202e-4690-be49-a754c6cf1399.json is GPT Image 2 (not Flare): 60143 ms, chatgpt.com, UND_ERR_SOCKET, no HTTP response. Disabling 2.5 cannot explain or resolve this failure.
- Existing worktree/planning history is dirty and retained. Search summaries require verification against fetched official documents before conclusions.
- Fetched official generation guide, tool guide and generate reference: GPT Image 2 and both 2.5 models allow existing prompt/background:auto/n:1/quality/size fields. 2.5 adds xhigh/max; GPT Image 2 must omit input_fidelity (adapter already does). GPT Image returns base64 without response_format. Nonstreaming is allowed; complex requests can take up to two minutes.
- Current models.jsonc has both 2.5 entries enabled:true (differs from earlier notes); preserve user configuration. Adapter hard-pins Codex ChatGPT endpoint and OAuth JWT/account header, NOT public API-key Images API. Need upstream Codex evidence before calling this endpoint incorrect.
- Official openai/codex codex-api/src/endpoint/images.rs confirms JSON POST images/generations and images/edits, including images:[{image_url:data URL}] for edits. Therefore Codex path/JSON format is not inherently wrong; public edit reference now ALSO supports JSON image references, so claiming multipart is mandatory would be incorrect. Upstream exposes x-codex-imagegen-request-id, currently not preserved in plugin diagnostics (secondary gap, cannot explain absent headers).
- Official Codex ext/image-generation/src/tool.rs currently pins gpt-image-2 and background/quality/size auto, omits n; shared schema accepts optional n. Its backend.rs sends x-codex-image-turn-id (plugin absent) plus originator. Plugin sends n:1 and its own originator/User-Agent; these differences are candidates for controlled compatibility comparison, not proven causes. Current upstream ImageQuality enum only has low/medium/high/auto, so public 2.5 xhigh/max support still cannot certify this Codex route.
- Re-ran pinned baseline comparison: all 30 scenarios passed for original GPT Image 2 URL/method/headers/serialized body; no credential access or real generation. Existing redirect:error difference would yield a redirect error if encountered, not by itself demonstrate this socket closure.
- Current child shell has HTTP(S)_PROXY configured to loopback HTTP port 7897, Node 22.23.1 / bundled Undici 6.27.0. Pi dist/core/http-dispatcher.js installs npm Undici EnvHttpProxyAgent globally and installs matching fetch; main.js calls it on startup and after settings load. Therefore no basis for claiming the plugin ignores proxy env. User httpIdleTimeoutMs is unset (default 300000), httpProxy is set; only allowlisted settings were inspected, no credentials.
- Credential-free GET probes using that same host dispatcher (not the already-running Pi process) got ChatGPT image route 403 Cloudflare HTML in 440 ms, public /v1/models 401 JSON in 565 ms. This establishes proxy/TLS/HTTP reachability now, not image authorization or live POST compatibility. No prompt, Authorization, image POST or retries were sent.
- Transport uses global fetch, JSON/nonstreaming, no per-request dispatcher and no retries. RPC timeout is 300000 ms; the 60143 ms socket close is not that timeout. UI connect phase means fetch has not returned headers, not proof TCP was never established.
- Current npm test: 29/31. model-config.test.ts assumes both 2.5 models disabled and rejects Flare as default; user config currently enables both, causing two failures unrelated to socket I/O. No configuration rollback or test change performed.
- Final report: plans/openai-image-request-investigation.md. Investigation only; live root cause cannot be assigned without authenticated controlled comparison. No mandatory request-body correction established.
## Regression investigation against the working commit
- Read only the user-named diagnostic file: gpt-image-2.5-flare, 60307 ms, UND_ERR_SOCKET / other side closed, no response headers. About 60 seconds is not the plugin's RPC timeout (300 seconds); TUI still uses the user's cancellation signal. Server/proxy/request compatibility cannot be distinguished from this log alone.
- HEAD e08e3f9 contains the same image plugin code as 4df3ccd805ef32616ccbc8ee635649b3be1f1e46. That version only sends gpt-image-2 for OpenAI.
- Re-fetched official Flare/Sunburst pages: public Image API/Responses model support is documented, but no proof that chatgpt.com/backend-api/codex/images accepts them under this OAuth account. Earlier mock tests checked parameters, not backend compatibility; default rollout was insufficiently gated.
- New repository-only `test/compare-baseline.mjs` executes actual pinned old and current extensions with identical synthetic auth/input, captures all fetch calls, and compares URL/method/headers/serialized body and exactly one auth resolution. Initial run caught missing direct-adapter quality:auto fallback; restored it. This difference does NOT by itself explain the Flare failure, because command configuration already resolves quality.
- After correction, all 30 cases match the working legacy request contract. Deliberate redirect:error security change remains and is explicitly asserted separately; no unsafe redirect restoration, proxy rewrite, TLS bypass, timeout increase or generation retries.
- Generic optional enabled flag retains both new model definitions but disables them by default. Menus/runtime exclude disabled entries, CLI retains key recognition and rejects locally, defaultModel must be enabled. Explicit opt-in only changes local permission, not backend support.
- Actual closure root cause remains unproven. Need user-confirmed live comparison on the legacy model before claiming full network/service recovery; no paid probes were run.
## Network diagnostics follow-up
- Bare fetch calls in both adapters and image downloader can throw Node TypeError('fetch failed') with useful nested `cause` / AggregateError details.
- runtime.generateWithTransport and generateAndSave reduce failures to `.message`, permanently discarding cause/code/stack; no diagnostic file is written.
- HTTP error parsing currently loses status when a message exists; JSON parse can also mask an HTML 403/502 response. Download-stage failures are indistinguishable from generation failures.
- User's actual network root cause is not yet known. Do not infer bad credentials from fetch failed; preserve transport details and avoid automatic POST retries/paid probes.
- Added shared transport wrapper around fetch AND body consumption. Distinguishes generate/edit/download and connect/response; records bounded recursive cause/AggregateError chain, code/syscall, duration, host-only endpoint, model, status and allowlisted request ID.
- Per-failure logs: current project's `.pi/image-generation-logs/error-<uuid>.json`; unique atomic private files, symlink directory checks, best-effort write failure notice. No successes/cancellations logged, deadlines are logged. Logger is closed over per-generation runtime and never enters SDK metadata or requests.
- Redact known keys/header values/prompt/reference base64 before truncation; also encoded secrets, JWT/Bearer strings, URLs and terminal controls. Do not serialize stack, socket, headers, body, process environment or arbitrary error properties. No retries, TLS bypass or proxy-setting changes.
- Validation complete: 30 offline tests + host suite. Host proves both providers preserve UND_ERR_CONNECT_TIMEOUT across SDK error flattening; RPC handler preserves ENOTFOUND/DNS guidance and a readable log reference under 800 chars, without recording the synthetic prompt/token. Only one fetch and one log per failed request.
- Logs cover transport and response-consumption failures, not pre-request validation or later image-format/local-save failures. Redaction is defensive/best-effort for arbitrary upstream messages: README instructs review before sharing, never send auth.json. Logs are small per-failure files and manually cleaned, not rotated.
## Image-first browser preview
- `src/gallery.ts` is the static local browser page. Current UI exposes a long local-file/developer explanation, filenames, provider/model strings; every image is constrained to 320px tall.
- Fresh Vercel Web Interface Guidelines fetched. Relevant: intrinsic image dimensions, eager/high-priority first image, lazy below-fold images, accessible links/focus, safe areas, responsive content and intentional theme.
- Keep script-free CSP, encoded relative paths, atomic persistence and no auto-opening browser. Metadata belongs in documentation, not the image canvas.
- Implemented quiet dark canvas, viewport-height single image, two-column desktop/one-column narrow-screen gallery, object-fit contain, always-visible original links without image overlays. Width/height attributes reserve the viewing box (not claimed intrinsic metadata); CSS contain preserves originals. No JS or animations.
- Original links retain encoded relative filenames; visible filenames/model/provider/prompt are omitted. Existing generated HTML is immutable; README notes only new galleries receive the design.
- Browser-tested nine viewport/layout combinations with synthetic landscape/portrait/square/panoramic PNGs. No overflow; images decode with contain sizing; single-image actions fit viewport. Inspected desktop/mobile screenshots.
- Chromium ignores the download attribute for local file URLs and navigates to the image instead (confirmed with focused navigation probe). Removed misleading download action instead of adding JS/loosening CSP. Images are already saved; original view supports native browser save.
- Final guideline fixes: `work/scripts/pi/pi-image-generation/src/gallery.ts:46` — reserved image geometry, first-image high priority, lazy remaining images, labeled native links; `:79` — visible keyboard focus; `:87` — responsive unclipped canvas; `:116` — skip navigation and minimal header. No animation/JS/form patterns introduced.
- Final Chromium rerun passed including both original entry points, special-character local filenames, keyboard skip and forced colors, with network offline and no console/page errors. Safari/Firefox and physical notched-device behavior not tested.
- Initial broad search hit Windows `nul` again; all further searches scoped to plugin.
## Duplicate-link follow-up
- Same gallery URL appeared in both durable image-entry renderer and Saved notification. This is duplicate presentation in publishOutcome, not a second generation call in that function.
- TUI now keeps link only on first image entry. Non-TUI notification retains plain URL. Saved counts/paths, gallery creation and RPC behavior unchanged.

## Latest OpenAI image model follow-up
- Current catalog has GPT Image 2 and two Wan models; provider default uses stable key `openai`.
- Official API model page confirms `gpt-image-2.5-sunburst` (snapshot 2026-09-08); tool guide also names `gpt-image-2.5-flare`. Both add `xhigh`/`max` quality with default `auto`.
- Sources: https://developers.openai.com/api/docs/models/gpt-image-2.5-sunburst and https://developers.openai.com/api/docs/guides/tools-image-generation .
- Official image-generation guide confirms both 2.5 models support generation/editing and the same dimension rules already configured for GPT Image 2: multiples of 16, ratio <=3, edge <=3840, 655360..8294400 pixels. Above 2560x1440 is experimental.
- Append keys `openai-sunburst` / `openai-flare`; leave all existing entries/provider defaults untouched. Retain conservative local prompt/input limits rather than claim expanded backend support.
- Existing adapter already passes arbitrary model/quality through. It uses the Codex subscription backend, not public API; official API support does not establish account/backend availability.
- Preserve substantial uncommitted changes, existing model entries and account settings.
## Follow-up: real hyperlinks
- Root cause confirmed: both entry and notification were plain Text URLs, with no OSC 8 or link styling.
- Host pi-tui exports hyperlink(text, url), getCapabilities(), getOsc8LinkAtColumn() and ANSI-safe Text wrapping. Pi detects Windows Terminal via WT_SESSION; traditional Windows console defaults hyperlinks=false.
- Use host hyperlink API only when detected supported; underline + theme mdLink color, retain visible full URI. TUI notification gets same formatting; RPC remains escape-free. HTML/file opening uses terminal + OS file association, not a plugin-launched browser.
- SDK Text preserves OSC 8 across wraps; verify target per visible cell and closure at end so link does not bleed to surrounding text.

- Existing extension already uses registerEntryRenderer + appendEntry for TUI image display.
- publishOutcome currently appends each image then notifies Saved paths.
- Repository has substantial pre-existing edits in this plugin; do not revert them.
- Browser preview must be opt-in; no process launch or local web server.
- Reuse existing ensureSafeOutputDirectory checks for .pi/generated-images; write unique HTML atomically and keep saved images intact on gallery failure.
- Persist optional galleryPath on first image entry to preserve link after session reload; older entries remain compatible. URI conversion uses Node pathToFileURL.
- Gallery uses escaped metadata and encoded relative image filenames, static CSS, no JS/external resources. Test single/multiple images, special characters, symlinks, failure fallback and host rendering.
